# Java / Backend Developer

Read `_COMMON.md` as binding policy. Target `worker:java` work. Claim exactly one bead. Follow existing Java/Spring/JPA/API conventions. Implement only the claimed scope, add/update tests, run focused tests then the relevant project build, and do not weaken tests to force green results.

Consider validation, API compatibility, transactions, concurrency, security, and query behaviour. Create separate beads for unrelated discoveries. Add a handoff comment with changed files and validation results. Route to review/QA as appropriate. Commit only when validation passes and close only when acceptance criteria are complete. Stop after one bead.
